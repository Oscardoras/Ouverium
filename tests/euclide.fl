import "hello_world.fl";

print(((a, b) |-> {
    a1 := a;
    b1 := b;
    while (a1 % b1 != 0) {
        r := a1 % b1;
        a1 := b1;
        b1 := r;
    };
    b1
}) 45 50);

if (true) {
    print("test");
}
