using "engine/parser/Standard.fl";


file := InputFile("test.fl");

str := String "";
while (file->has()) {
    s := file->read();
    str :+ (String s + String "\n")
};

words := get_words(str);
print(words)