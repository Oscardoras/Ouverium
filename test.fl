using "engine/parser/Standard.fl";


file := InputFile("test2.fl");

str := String "";
while (file->has()) {
    s := file->read();
    str :+ (String s + String "\n")
};
print(str);

words := get_words(str);
words->foreach(x |-> {
    print(x, "\n")
});
