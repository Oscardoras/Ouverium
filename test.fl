using "librairies/data_structure/ArrayList.fl";

list := ArrayList();
list->add_last(1,1);
list->add_last(2,2);
list->add_last(3,3);
list->add_last(4,4);

it := list->iterator();
while (it->has_next()) {
    print(it->get(), "\n");
    it->next()
}