(:~) : (object, type) |-> {
    add(object->_types, type);
    object
};

(~) : (object, type) \ (type !== Char & type !== Int & type !== Float & type !== Bool) |-> {
    found := false;
    l := lenght(object->_types);
    i := 1;
    while (!found & i <= l) repeat {
        if (get(object->_types, i) = type) then {
            found := true
        } else {
            i :=+ 1
        }
    };
    found
}