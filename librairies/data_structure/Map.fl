using "Iterable.fl";


Map : this |-> {
    BidirectionalIterable(this);
    this :~ Map;

    this : key |-> {};

    this->lenght : () |-> {};
    this->is_empty : () |-> {};

    this->has : key |-> {};
    this->remove : key ? (this->has(key)) |-> {};

    this->iterator : key ? (this->has(key)) |-> {};

    this
}