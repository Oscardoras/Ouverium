using "Expression.fl";


Property : (this, object, name) \ (object ~ Expression & name ~ String) |-> {
    Expression(this);
    this :~ Property;

    this->object := object;
    this->name := name;

    this
};

Property : (object, name) \ (object ~ Expression & name ~ String) |-> {
    Property((), object, name)
}