using "Expression";


Symbol : (this, name) \ (name ~ String) |-> {
    Expression(this);
    this :~ Symbol;

    this->name := name;

    this
};

Symbol : name \ (name ~ String) |-> {
    Symbol((), name)
}